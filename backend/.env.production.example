# Production Environment Variables for EC2 Deployment
# Copy this file as .env and set appropriate values for your environment

# Database Configuration (MySQL)
# Railway Database Configuration
DB_URL=jdbc:mysql://switchback.proxy.rlwy.net:19205/railway
DB_USERNAME=root
DB_PASSWORD=UWAJtVPuysnmzNHdXkvcobkVRYKuuVAc

# JWT Configuration
# Generate a strong secret key (at least 32 characters)
JWT_SECRET=your_production_jwt_secret_key_at_least_32_chars_long_change_this
JWT_EXPIRATION=86400000

# JPA DDL Auto Configuration
# Use 'validate' in production to prevent accidental schema changes
JPA_DDL_AUTO=validate

# Logging
LOG_FILE_PATH=/var/log/quiz-app/application.log

# CORS Configuration
# Set to your frontend domain in production
CORS_ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# Mail Configuration (Optional)
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-app-password

# Server Port
SERVER_PORT=8080

# Server Timezone
TZ=UTC
