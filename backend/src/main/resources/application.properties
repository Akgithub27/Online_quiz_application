spring.application.name=quiz-app
server.port=8080

# MySQL Railway Database Configuration
spring.datasource.url=jdbc:mysql://switchyard.proxy.rlwy.net:19594/railway
spring.datasource.username=root
spring.datasource.password=mWmVkeSofWZDgtcMlrwkfhyVVWVmCCDz
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.jdbc.batch_size=20
spring.jpa.properties.hibernate.order_inserts=true
spring.jpa.properties.hibernate.order_updates=true

# JWT Configuration
jwt.secret=your_super_secret_jwt_key_change_this_in_production_min_32_chars_long_key123
jwt.expiration=86400000

# Logging
logging.level.root=INFO
logging.level.com.online_quiz=DEBUG

# CORS Configuration
app.cors.allowed-origins=http://localhost:5173,http://localhost:3000,https://yourdomain.netlify.app
app.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
app.cors.allowed-headers=*
app.cors.max-age=3600
app.cors.allow-credentials=true

# Mail Configuration (Optional - for email notifications)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:your-email@gmail.com}
spring.mail.password=${MAIL_PASSWORD:your-app-password}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# OpenAPI/Swagger Configuration
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.enabled=true
springdoc.swagger-ui.operations-sorter=method
springdoc.swagger-ui.tags-sorter=alpha

# Jackson Configuration
spring.jackson.serialization.write-dates-as-timestamps=false
spring.jackson.time-zone=UTC
